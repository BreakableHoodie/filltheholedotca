.svelte-kit/
build/
node_modules/
playwright-report/
test-results/
.worktrees/
.vercel/
.netlify/
supabase/
docs/
# Agent/prompt instruction files, workflows, and issue templates. Same rationale
# as docs/: authored prose, not application code. Reformatting them adds large
# diffs to unrelated PRs and drags automated reviewers across pre-existing
# content that the PR did not actually change.
.github/
*.sql
**/pnpm-lock.yaml
**/package-lock.json

# Frozen open-data snapshots served by the parked worker — archives, do not reformat.
parked/public/api/
